export const getCategoryLabel = (category, t) => {
  return t(`common.categoryValue.${category}`);
};

export const getSkillLabel = (skillLevel, t) => {
  return t(`common.skill.${skillLevel}`);
};

export const getStatusLabel = (status, t) => {
  return t(`common.status.${status}`);
};
